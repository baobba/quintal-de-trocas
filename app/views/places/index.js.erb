$('.cards').append("<%= escape_javascript(render :partial => @places)%>");
$('#load_more_link').replaceWith("<%= escape_javascript(link_to_next_page(@places, 'Veja mais', :remote => true, :id=>'load_more_link'))%>");