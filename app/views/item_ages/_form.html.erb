<%= form_for(@item_age) do |f| %>
  <% if @item_age.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@item_age.errors.count, "error") %> prohibited this item_age from being saved:</h2>

      <ul>
      <% @item_age.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
