<%= form_for(@item_image) do |f| %>
  <% if @item_image.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@item_image.errors.count, "error") %> prohibited this item_image from being saved:</h2>

      <ul>
      <% @item_image.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :item_id %><br>
    <%= f.text_field :item_id %>
  </div>
  <div class="field">
    <%= f.label :image %><br>
    <%= f.text_field :image %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
