<%= form_for(@toy_category) do |f| %>
  <% if @toy_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@toy_category.errors.count, "error") %> prohibited this toy_category from being saved:</h2>

      <ul>
      <% @toy_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
