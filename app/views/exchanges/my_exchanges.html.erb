<div class="row">
  <div class="col-sm-2">
    <%= render '/layouts/nav_dashboard' %>
  </div>
  <div class="col-sm-10">
    
  
<div class="page-header">
  <h1>Minhas trocas</h1>
</div>

<table class="table">
  <thead>
    <tr>
      <th>Título</th>
      <th></th>
      <th>Envio</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @exchanges.each do |exchange| %>
      <tr>
        <td><%= exchange.created_at %></td>
        <td>De: <%= exchange.toy.title if exchange.toy %></td>
        <td><%= exchange.exchange_type %></td>
        <td><%= link_to 'Show', exchange %></td>
        <td><%= link_to 'Edit', edit_exchange_path(exchange) %></td>
        <td><%= link_to 'Destroy', exchange, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Place', new_place_path %>

</div>
</div>
